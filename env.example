# Midscene 测试 - 环境配置模板
# 将此文件复制为 .env 并填入你的实际值

# ========================================
# Android 应用包名
# ========================================
# 格式：package.name/activity.name
# 查找包名：adb shell pm list packages | grep <app-name>
# 查找 Activity：adb shell dumpsys package <package-name> | grep -A 1 "android.intent.action.MAIN"

# Sauce Labs 演示应用
ANDROID_SAUCE_PACKAGE=com.swaglabsmobileapp/com.swaglabsmobileapp.MainActivity

# 视频编辑器应用
ANDROID_EDITOR_PACKAGE=vidma.video.editor.videomaker/com.atlasv.android.mvmaker.mveditor.LaunchActivity

# 视频下载器应用
ANDROID_DOWNLOADER_PACKAGE=free.video.downloader.converter.music/free.video.downloader.converter.music.view.activity.StartupActivity

# Facebook 应用
ANDROID_FACEBOOK_PACKAGE=com.facebook.katana/com.facebook.katana.LoginActivity

# CallMe 应用 - 使用空字符串或实际包名
ANDROID_CALLME_PACKAGE=

# TextNum 应用 - 使用空字符串或实际包名
ANDROID_TEXTNUM_PACKAGE=

# 录音应用 - 使用空字符串或实际包名
ANDROID_RECORDER_PACKAGE=

# 播放器应用 - 使用空字符串或实际包名
ANDROID_PLAYER_PACKAGE=

# ========================================
# iOS 应用 Bundle ID
# ========================================
# 格式：com.company.appname
# 查找 bundle ID：ideviceinstaller -l

# 视频编辑器应用
IOS_EDITOR_BUNDLE_ID=

# TextNum 应用
IOS_TEXTNUM_BUNDLE_ID=

# 邮件应用（系统应用）
IOS_MAIL_BUNDLE_ID=com.apple.mobilemail

# ========================================
# 测试配置
# ========================================

# 启用/禁用 AI 操作缓存（加速重复测试）
MIDSCENE_CACHE=false

# 默认测试超时（毫秒）
# 可在单个测试文件中覆盖
TEST_TIMEOUT=240000

# ========================================
# 飞书通知配置
# ========================================

# 飞书机器人 Webhook URL
# 从飞书机器人设置中获取
# 示例：https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
FEISHU_WEBHOOK=

# 禁用飞书通知（本地开发时很有用）
# 设置为 'true' 禁用，'false' 或留空启用
DISABLE_FEISHU_NOTIFY=false

# ========================================
# Surge 配置（报告托管）
# ========================================

# Surge 凭据用于上传测试报告
# 从 https://surge.sh/ 获取令牌
# 或使用：npx surge login 登录

# 你的 Surge 邮箱
SURGE_EMAIL=

# 你的 Surge 令牌（登录后获取）
SURGE_TOKEN=

# 自定义报告托管域名（可选）
# 如果不设置，将生成随机域名
# 示例：my-team-test-reports
SURGE_DOMAIN=

# ========================================
# Web 应用 URL（可选）
# ========================================
# 如果测试不同环境，可覆盖默认 URL

# Sauce Labs 演示
# SAUCE_DEMO_URL=https://www.saucedemo.com

# eSIM 门户
# ESIM_URL=https://esimnum.com/home

# ========================================
# 高级配置
# ========================================

# Android ADB 设置
# ADB_HOST=localhost
# ADB_PORT=5037

# iOS WebDriverAgent 设置
# WDA_PORT=8100
# WDA_HOST=localhost

# Web 测试的浏览器设置
# HEADLESS=false
# BROWSER=chromium

# 并行测试执行
# TEST_WORKERS=1

# 截图设置
# SCREENSHOT_ON_FAILURE=true
# SCREENSHOT_DIR=./screenshots

# ========================================
# 说明
# ========================================

# 1. 此文件不应提交到 git（.env 已在 .gitignore 中）
# 2. 所需变量取决于你运行的测试类型：
#    - Web 测试：无需特殊配置
#    - Android 测试：需要 ANDROID_*_PACKAGE 变量
#    - iOS 测试：需要 IOS_*_BUNDLE_ID 变量
# 3. 飞书和 Surge 配置是可选的
# 4. 对于不需要明确包名规范的应用，使用空字符串 ''
# 5. 以 # 开头的注释会被忽略
